h2 Dougo and Marko's awesome tail dashboard

script(src="http://localhost:9000/socket.io/socket.io.js", type="text/javascript")
script
  var socket = io.connect('http://localhost:9000');
  socket.on('tail', function (data) {
    $('#lines').append(data.line + "<br />");
    console.log(data.line);
    socket.emit('my other event', { my: 'data' });
  });

div#lines
